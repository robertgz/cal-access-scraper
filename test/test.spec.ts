import { getFilings } from '../src/filings'
var assert = require('chai').assert

describe('Filings', () => {
  // it('should return an array', async function () {
  //   this.timeout(40000);
  //   const filings = await getFilings('6/4/2022');
  //   // expect(filings).to.equal('promise resolved')
  //   assert.typeOf(filings, 'array', 'filings is an array'); // with optional message
  // });
});
